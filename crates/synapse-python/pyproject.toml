[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "synapse-mnm-client"
version = "0.1.0"
description = "Python SDK for Synapse: Configuration Language for Memory Systems"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
